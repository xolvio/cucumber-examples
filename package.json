{
  "name": "cucumber-example",
  "version": "0.0.0",
  "description": "Example project showing how to run cucumber with webdriver.io",
  "author": "Sam Hatoum",
  "private": true,

  "repository": {
    "type": "git",
    "url": "https://github.com/xolvio/cucumber-examples.git"
  },

  "dependencies": {
    "cucumber": "0.4.8",
    "webdriverio": "2.4.5"
  },

  "scripts": {
    "kill-phantom": "pkill -9 phantomjs || true",
    "start-phantom": "nohup phantomjs --webdriver 4444 > /dev/null 2>&1 &",
    "wait-for-phantom": "sleep 1",
    "run-cucumber": "./node_modules/cucumber/bin/cucumber.js -f pretty",
    "test": "npm run kill-phantom && npm run start-phantom && npm run wait-for-phantom && npm run run-cucumber && npm run kill-phantom"
  }
}